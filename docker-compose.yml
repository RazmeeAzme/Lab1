version: "3.9"

services:
  python-app:
    build: .
    container_name: python_app_container

  web:
    build: ./web
    container_name: web_container
    ports:
      - "8080:80"
    depends_on:
      - python-app
    volumes:
      - web_logs:/var/log/nginx

volumes:
  web_logs: